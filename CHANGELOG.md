# Changelog

Todas as mudanças notáveis deste projeto são documentadas neste arquivo.

O formato segue [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/) e o versionamento segue [Semantic Versioning](https://semver.org/lang/pt-BR/).

## Convenção obrigatória deste projeto

**Toda mudança em regra de decisão — elegibilidade, pesos, penalidades, cotas, ordem de relaxamento — é obrigatoriamente registrada aqui.** A comparação entre semanas depende de saber qual configuração produziu cada lista; uma mudança de regra sem registro torna duas rodadas incomparáveis sem que ninguém saiba. O registro aqui complementa, não substitui, a entidade `alteracao_parametro` do Registro.

## [Unreleased]

### Added

- **A penalidade §6.4 "janela anterior sem resultado" passa a INCIDIR — o consumidor da sexta lê o Registro (regra de decisão).** A fatia anterior deu produtor a `registro.janela_destaque`; esta liga o consumidor, e com isso a terceira das três penalidades da Spec §6.4 deixa de ser inerte. Até aqui a coluna saía `0,0` para todo imóvel — não porque ninguém merecesse o desconto, mas porque nada alimentava o cálculo, o que é indistinguível de "nenhum imóvel penalizado".

  **A leitura acontece no nó do DECISOR**, não no Coletor Interno, porque a Spec §5 é explícita: "o Decisor é o único agente que lê o Registro durante a rodada, e o faz para obter o histórico de janelas necessário ao cálculo da penalidade". O Coletor Interno segue lendo só o Newcore.

  **Ler e julgar são separados, e isso é de diagnóstico, não de estilo.** O nó **sempre lê** o histórico; só **julga** quando o limiar por nível existir. O estado passa a carregar `janelas_lidas`, e a planilha declara duas limitações distintas: "histórico vazio" (o Registro não devolveu janela encerrada) e "limiar não definido" (o nº 14 é nulo). Sob um sinal só, as duas sairiam idênticas — e elas zeram a penalidade por motivos **opostos**, com correções opostas, das quais só uma está sob controle do dono.

  **O limiar é injetado, nunca constante.** `julgar_janelas` é pura (`src/dominio/penalidades.py`) e recebe o mapa por nível como argumento obrigatório; **nível ausente no mapa é erro, não default** — julgar uma janela de super destaque pela régua do destaque é exatamente o que a §6.4 proíbe ao dizer "para o nível". Nenhum valor de parâmetro pendente entrou em `src/config` nem no domínio: o nº 14 segue nulo, e por isso **nenhuma janela é julgada hoje**. O que mudou é que a fiação existe e a planilha diz qual das duas coisas falta; declarados os dois limiares no arquivo da rodada, a penalidade acende sem mudança de código.

  **D-023 — a §6.4 passa a julgar só a ÚLTIMA janela (regra de decisão, decidida pelo dono).** O código penalizava se *qualquer* janela encerrada tivesse falhado, sem recorte temporal; o PRD fala em "o resultado da **sua última janela**" e a Spec §6.4 no singular "janela **anterior**". Sob a regra antiga, uma janela ruim de um ano atrás penalizava para sempre — e com decaimento de razão 1.0, sem nunca esmaecer. Pior em combinação com a D-021: a promoção de nível fecha a janela, então subir para super destaque criava uma janela curta de destaque que quase nunca bate o limiar e passaria a penalizar indefinidamente **justo o imóvel que subiu por mérito**. Custo aceito e declarado: um histórico ruim longo "limpa a ficha" com uma única janela boa. "Mais recente" é o menor `ciclos_desde_encerramento`, não a posição na lista (a ordem é do chamador e não pode governar a regra); empate de ciclos é desempatado pela janela que FALHOU — desempate DEFENSIVO, **hoje inalcançável**: o produtor da D-021 fecha no máximo uma janela por imóvel por carga, e como todo fim é data de carga aprovada a contagem de ciclos as separa por pelo menos 1. Ele existe só para a função ser total, e para que a regra não passe a depender da ordem da lista se alguma premissa mudar. Efeito colateral bom: o predicado e o desconto passam a olhar a MESMA janela, que antes divergiam.

  **D-023 — Registro indisponível DEGRADA e entrega, em vez de abortar (decidida pelo dono).** Uma falha ao ler o Registro derrubava a rodada inteira: nenhuma planilha, semana sem vitrine, por causa de **uma** das três penalidades. A Spec §7.2 descreve exatamente o caso ("alguma fonte falhou e a decisão prosseguiu com dado parcial") e nenhum documento sancionava o aborto, que acontecia por omissão. A etapa `janelas` entrou em `ETAPAS_PARA_COMPLETA` — sem ela, uma rodada cujo Registro caiu sairia COMPLETA com a penalidade silenciosamente inerte.

  **Guardas sobre o limiar, que o dono declara.** Zero é recusado: `leads >= 0` é sempre verdadeiro, a coluna sairia 0,0 para todos e nenhuma limitação seria emitida — a D-022 é explícita, "nunca 0,0 silencioso"; para desligar a penalidade, omite-se a seção. E `super_destaque` precisa ser MAIOR que `destaque`: o PRD fixa que "o resultado suficiente é proporcional ao tipo de posição — super destaque exige entrega superior", e sem a guarda um arquivo com a régua invertida carregaria em silêncio.

  O recorte da rodada ganhou **teto na data de referência, nas duas pontas**: quais janelas entram e quantos ciclos se contam. Sem ele, a mesma sexta reprocessada com `--hoje` no passado julgaria janelas encerradas DEPOIS da sua data de referência — que não são "janela anterior" — e todas cairiam em ciclos 0, empatadas; o desempate então faria a regra voltar a se comportar como o `any` que a D-023 acabou de remover, em silêncio e com penalidade cheia. Teto em metade do mecanismo era pior que nenhum. `janelas_lidas` virou `int | None` para separar "não consultado" de "consultado e vazio" — a limitação afirma o segundo, e sob um zero só ela afirmaria uma consulta que não houve. E `--dry-run` voltou a não tocar o Registro, como promete — com uma consequência declarada: o ensaio passa a sair sempre DEGRADADA, porque `janelas` está entre as etapas exigidas e o ensaio não consulta o Registro. A limitação "REGISTRO NÃO CONSULTADO" explica por quê, para que quem opera não confunda com problema real.

  Verificado por mutação: desligar o acoplamento das janelas faz a penalidade sumir do detalhamento do imóvel; trocar `>=` por `>`, remover a checagem de nível, fixar o contador em zero, devolver janelas em curso e inverter o veredito também mordem. Com contraprova — uma janela que ATINGIU o resultado não é penalizada, senão o teste passaria mesmo com o código penalizando toda janela indistintamente. E o conjunto de imóveis enviado ao Registro passou a ter trava própria: o revisor o mutou para lista vazia e a suíte inteira passava.

- **`registro.janela_destaque` ganha PRODUTOR — a D-020 passa a valer de fato, e a cadência semanal fecha o ciclo sobre si mesma (D-021, D-022).** A tabela existia desde o DDL inicial e **ninguém escrevia nela**. As duas consequências eram silenciosas: as colunas "semanas consecutivas em destaque" e "leads acumulados na janela atual" (Spec §4.3) ficavam vazias **indefinidamente** — não "nas primeiras semanas", como a D-020 previa —, e a penalidade por janela anterior sem resultado (§6.4) não tinha insumo, saindo 0,0 para todo imóvel, indistinguível de "nenhum imóvel penalizado".

  **D-021 — quando a janela abre, acumula e fecha (regra de decisão, decidida pelo dono).** Nenhum documento definia isso: a Spec §2.1 nomeia os campos ("início e fim = datas de entrada e saída da vitrine") e nunca diz quem observa a saída. A janela agora fica **aberta enquanto o imóvel continuar aparecendo nas cargas aprovadas**, acumulando os leads de cada período e incrementando as semanas; fecha quando uma carga nova não o traz mais. É o que casa com o dado medido — janela média de **33 dias** contra 7 de ciclo de carga — e a única leitura que dá sentido a `semanas_consecutivas`, que sob a alternativa (uma janela por ciclo) seria sempre 1. Efeito sobre a §6.4: a janela julgada é o período **inteiro** de exposição, não a última semana.

  **D-022 — "resultado esperado para o nível" vira o parâmetro pendente nº 14**, com dois valores (super destaque e destaque), nulo. A §6.4 exige o julgamento *por nível* e nenhum documento o quantifica; o `src/dominio/penalidades.py` já se recusava a inventá-lo. Descartado "pelo menos 1 lead nos dois níveis": tem apoio descritivo no PRD, mas julga os dois níveis pela mesma régua contra a letra da §6.4 e penalizaria **88%** da base — uma penalidade quase universal perde o poder de discriminar que a §6.4 lhe atribui. **A tabela de parâmetros do CLAUDE.md passa de treze para quatorze; os pendentes, de doze para treze.**

  **Onde o produtor roda**: dentro de `gravar_acompanhamento`, na MESMA transação (PRD, passo 5 do ciclo de segunda: "Registro | Resultado da carga | Acumulação do resultado por janela de destaque"). Transação única de propósito — uma rodada gravada cuja janela não acumulou faria o relatório seguinte contar uma permanência a menos e a §6.4 julgar por um acumulado incompleto. `gravar_acompanhamento` passa a devolver `(rodada_id, histórico)`, e o histórico vem de lá, não de uma leitura à parte, porque ele só existe **depois** de a mesma transação acumular: uma leitura separada veria o estado anterior e o relatório diria "2 semanas" na terceira.

  **A unidade de acumulação é a CARGA, não a execução da segunda** — e isso é regra, não detalhe. A guarda de idempotência é `ultima_rodada_decisao_id`. Uma primeira versão chaveava pela rodada de acompanhamento e **não guardava nada**: `gravar_acompanhamento` abre uma rodada nova a cada execução, com id novo, então reprocessar somava os leads outra vez — e o teste passava porque chamava duas vezes com o mesmo id, cenário que a fiação real nunca produz. Achado dos três portões, no mesmo caminho que a operação de fato percorre (o runner commita o Registro e só então escreve a planilha; se a planilha falha, a retomada documentada é rodar de novo). Chaveando pela carga, cai junto o outro defeito: duas segundas medindo a MESMA carga — concreto enquanto nada carimba `aprovada_em` — contavam duas semanas para uma carga só.

  **As datas são as da carga, não as do relógio.** `inicio`/`fim` vinham de `datetime.now()`, o que deslocaria toda janela em dias e, num reprocessamento, carimbaria o Registro com a data de hoje. Passam a vir de `aprovada_em` (via `resumo.inicio_periodo`) — o melhor proxy que existe, já que o sistema não publica nada e a carga é manual.

  **Migração 005**: `rodada_decisao_id` (qual carga abriu a janela), `ultima_rodada_decisao_id` (a guarda acima), `ultima_rodada_acompanhamento_id` (rastreabilidade), **índice único parcial** garantindo no máximo uma janela ABERTA por imóvel (duas somariam leads numa e semanas na outra sem nada acusar), CHECKs de coerência e índice para a leitura da sexta. O passo de CI que valida o DDL passa a parsear **todas** as migrações, não só a 001 — elas são aplicadas à mão, então um erro de sintaxe numa migração nova só apareceria no banco de quem opera; e falha se o glob vier vazio, para não passar guardando nada.

  **Quatro elaborações da D-021, todas declaradas em `docs/decisoes.md`** porque o documento não as cobre: mudança de nível fecha a janela e abre outra (a §6.4 julga por nível, e uma janela que atravessasse os dois seria julgada pela régua errada); carga retroativa é recusada com mensagem própria; `desempenho` vazio é recusado (fecharia TODAS as janelas da base); e "ciclo", para o decaimento, é uma carga **aprovada** — uma sexta abortada não expôs imóvel nenhum.

  **Duas limitações declaradas na planilha da segunda**, porque são números de aparência factual sobre o que o sistema não sabe: os leads da janela são **amostra** (três dias medidos de um ciclo de sete, contra o "acumulado durante a janela" da §2.1 — quando o nº 14 existir, julgará por número subestimado, penalizando a mais), e a contagem de semanas **começa agora**, sem o histórico anterior ao produtor.

  **A outra metade da D-022 saiu junto**: o nº 14 é declarável no TOML (`[resultado_esperado]`, a única seção opcional — ausência é o estado nulo que a decisão determinou, não um default inventado; quem declara, declara os dois níveis) e a rodada de sexta **declara** "limiar de resultado não definido" quando ele falta. Sem isso, o dono não conseguiria sequer declarar o parâmetro, e não penalizar por falta de régua seria indistinguível de passar no critério.

  **Planilha e Registro dizem a mesma coisa, e a limitação temporária expira sozinha.** As limitações do acúmulo vão à planilha **e** ao `motivo_degradacao` gravado (`gravar_acompanhamento` devolve `AcumuloDaJanela`): quem auditasse pelo banco — que a D-001 põe como fonte da verdade — não veria que os leads são amostra. E a de história rasa é **derivada**, não constante: vale enquanto houver janela em curso aberta na primeira carga que o produtor viu. Emitida incondicionalmente, em seis meses a planilha ainda afirmaria que a contagem está começando, sobre janelas com histórico real — a mesma classe de limitação falsa que a degradação incondicional de portal produzia e que este projeto já corrigiu duas vezes.

  **O CI passou a subir um Postgres.** Os testes de I/O do Registro pulavam em todo ambiente automatizado, e a fatia mostrou o custo: o defeito de idempotência vivia justamente na metade que ninguém exercitava. O job aplica as cinco migrações em ordem (provando, a cada PR, que a sequência sobe num banco limpo) e **falha se qualquer teste for pulado** — um skip silencioso é indistinguível de um teste que passou.

  **`ciclos_desde` deriva a data uma vez só, em Python.** Comparar com `aprovada_em::date` usaria o fuso da SESSÃO Postgres, enquanto `inicio`/`fim` da janela são gravados no fuso do SISTEMA: numa base em UTC com máquina em -03, toda carga aprovada depois das 21h cairia num dia diferente, e a carga que removeu o imóvel contaria como ciclo posterior ao fechamento que ela mesma produziu.

  `com_historico` (puro) preenche as duas colunas da §4.3 entre registrar e entregar, sem recomputar medição nenhuma — apurar duas vezes resolveria a ordem e criaria duas medições da mesma semana, livres para divergir. 21 testes novos. Verificadas por mutação: quebrar a costura entre `registrar` e a planilha devolve as duas colunas a `None` numa semana em que a janela acumulou; e trocar a data da carga pelo relógio faz `31/08` aparecer onde a carga é de `28/08` — as duas costuras que os portões apontaram como **sem trava nenhuma**.

- **Rodada de SEXTA (decisão) executável por comando — `src/executar/sexta.py` + `src/config/parametros.py` (novos).** Fecha a simetria com a segunda: as duas cadências do produto passam a ter ponto de entrada commitado (`rodada-sexta` / `rodada-segunda`), reproduzível e auditável — até aqui o grafo da decisão existia e passava nos testes, mas quem o invocava era script solto e não versionado (a planilha em `saida/piloto/` saiu de um desses). O runner fia Coletor Interno (com `DefinicaoAtivoDistrito.PRODUTIVOS`, D-015), dimensões, vendas e Coletor Externo; grava a rodada no Registro **antes** de escrever a planilha; e distingue falha de ESCRITA (código 1) de falha de FONTE (3), aborto por estoque vazio (4), **aborto por veto do crivo (6)** e parâmetros (5), com o 2 reservado ao argparse.

  **A questão de regra que esta fatia resolve: como uma rodada roda se doze dos treze parâmetros são nulos.** A resposta NÃO é um default "razoável" no código — seria exatamente o valor inventado que o CLAUDE.md proíbe, com o agravante de ficar invisível numa planilha aprovada. É `--parametros ARQUIVO.toml`, **obrigatório e sem default**: os valores são declarados pelo **dono da decisão** num arquivo fora do repositório, e o carregador **recusa a rodada** se faltar qualquer chave (erro nomeando o pendente: "parâmetro pendente nº 12") ou se houver chave desconhecida (um `decaimeto` digitado errado é erro, nunca um valor descartado em silêncio). `src/config` continua **sem valor nenhum** — ganhou o carregador, não os números. Os dois parâmetros que são FORMA e não número (decaimento da penalidade por janela, do nº 3; composição do sinal F3) escolhem-se numa **lista fechada de formas nomeadas** — expressão livre num arquivo de configuração seria código executável fora da revisão, e o invariante 5 deixaria de ser verificável.

  **Nada é adotado por carregar.** Tudo que entra sai rotulado PROVISÓRIO na planilha, com a ORIGEM do arquivo, e o TOML declarado vai **verbatim** para `parametros_da_rodada` do Registro — inclusive as duas formas, que viram função e não sobreviveriam a `ParametrosDecisao` — junto da **data de referência** e da **definição de gestor ativo**, que são entradas da decisão fora do TOML e sem coluna própria no Registro. Com isso os **parâmetros e o recorte** ficam reconstituíveis; o **estoque não**, porque as janelas de 30 e 180 dias saem de `NOW()` no SQL. Dizer "rodada reproduzível" seria a mesma classe de promessa vazia que esta fatia passou o tempo todo corrigindo — o invariante 5 vale sobre a mesma entrada, e "mesma entrada" inclui o banco no instante da rodada. Os doze pendentes seguem nulos na tabela do CLAUDE.md.

  **A aprovação NÃO é aberta pelo runner**: o que dispara a tácita sozinha é o prazo, parâmetro pendente nº 10, **nulo** — abrir uma thread de aprovação sem prazo afirmaria um prazo que ninguém definiu. O runner informa o `rodada_id` e quem tem o prazo (console/agendador) abre `grafo.aprovacao` com ele. **Sem raspagem** (`--externo` ausente), F3 não entra e a rodada sai degradada nesse fator, com a limitação declarada (§7.2); a fiação é tudo-ou-nada, e um teste prova que o runner nunca produz meia-fiação. 82 testes novos — o central varre **cada chave obrigatória, removendo-a uma a uma**, e exige que a rodada quebre: sem ele, um `bruto.get(chave, 0.5)` de conveniência nasceria com a suíte verde e a sexta sairia com um número que ninguém escolheu (verificado por mutação — injetar esse default derruba exatamente o caso correspondente). `src/config` entra no wheel: o gatilho previsto em `test_empacotamento.py` disparou no mesmo commit em que o primeiro `.py` do pacote nasceu; `mypy --strict` do CI passa a cobrir `src/config` e `src/executar` além de `src/dominio`.

  **Códigos de saída, com o veto do crivo separado do estoque vazio.** `0` entregou; `5` parâmetros (ausentes, fora de faixa, forma desconhecida, ou o arquivo-modelo); `4` abortada por **estoque vazio**; `6` abortada por **VETO DO CRIVO** — a auditoria reprovando a seleção: cota excedida, piso do super, relaxamento em super (invariantes 6 e 7), e também imóvel em dois níveis, posição sem justificativa e corte do ranking desonrado. Sob um código só, uma violação de invariante chegaria ao monitoramento com a mesma cara de "não havia imóvel para decidir" e ninguém iria olhar; `3` falha ao coletar/decidir; `1` falha de escrita; `2` reservado ao argparse. Dois passos de CI guardam a recusa: um TOML **existente com uma chave removida** (a primeira versão apontava para um caminho inexistente, que sai 5 pelo ramo de arquivo ilegível sem nunca chegar à validação — passo verde que não guardava nada) e o modelo ilustrativo.

- **Limitações que a planilha passou a DECLARAR, e uma que ela declarava FALSAMENTE (regra de decisão — Spec §3.1/§6.4/§6.6/§7.2).** Levantadas pelos três portões na fatia do runner e corrigidas junto com ela:

  **Falsa, agora condicional:** `DEGRADACOES` emitia "desempenho de portal ausente" **incondicionalmente**. Desde que o runner passou a poder alimentar F3 com raspagem viva, uma rodada COMPLETA declarava, duas linhas abaixo do próprio estado, que o portal estava ausente — limitação falsa na planilha que sustenta a aprovação. Agora só é emitida quando o F3 de fato não entrou.

  **Ausentes, agora declaradas:** (1) a **penalidade por janela anterior sem resultado não incide sobre ninguém** — o Coletor Interno devolve `janelas_anteriores=()` para todo imóvel e nada lê `registro.janela_destaque` (produtor inexistente), então uma das três penalidades da §6.4 é inerte e a coluna sai 0,0 para todos, indistinguível de "sem histórico" que o PRD manda identificar; a limitação é **derivada do resultado** e some sozinha quando o produtor chegar; (2) **razão 1.0 não decai**, divergindo da §6.4; (3) a **definição de gestor ativo do distrito** (D-015, cobertura 45,9%), que a D-016 já mandava declarar; (4) os quatro obrigatórios da §3.1 — **idade do dado do portal**, **taxa de amarração** (os dois eram descartados pelo grafo justamente no caso de SUCESSO, e só existiam embutidos no motivo da rejeição), **posições não preenchidas** e **variação de volume** (sem produtor, declarada como NÃO APURADA). Cada um que falta é declarado ausente: uma linha faltando é indistinguível de um número que ninguém calculou.

  **Aba nova — `relaxamento.csv`.** A Spec §6.6 é literal: "cada cedência gera linha no relatório de relaxamento com a quantidade de posições que dependeram dela. **Sem esse registro a etapa de decisão não é considerada pronta**", e a §3.1 a lista como obrigatória. O Registro já gravava o agregado por regra; ele só não chegava ao artefato que as pessoas leem, e a rodada saía COMPLETA assim mesmo. Traz também o **déficit** (posições que os cinco graus não cobriram), declarado mesmo quando zero.

  **Planilha e Registro dizem a mesma coisa.** As limitações de fiação vão para o `motivo_degradacao` gravado, não só para o CSV — sob a D-001 o Registro é a fonte da verdade e é por ele que a segunda enxerga a carga; se vivessem só na planilha, os dois artefatos discordariam sobre a mesma rodada. Fica em `docs/decisoes.md` a pergunta ao dono sobre se elas deveriam também mudar o ESTADO para degradada (a §7.2 argumenta que sim; contra, isso tornaria toda rodada degradada até o produtor de `janela_destaque` existir, e um estado que nunca varia deixa de informar).

  **Divergência aberta, registrada em `docs/decisoes.md`, não resolvida em silêncio:** o PRD e a Spec §5 põem o **Redator antes do Registro**; o código faz o inverso. Sob a D-001 isso importa — uma rodada gravada sem planilha pode ser aprovada por decurso de prazo e virar a carga vigente contra a qual a segunda mede. Aguarda o dono. Registrada junto a lacuna operacional da cadeia sexta → carimbo → segunda: nenhum componente existente carimba `aprovada_em`, então hoje toda segunda declararia ausência de carga.

- **Rodada de SEGUNDA (acompanhamento) — Monitor Operacional (M1+M2).** `src/dominio/acompanhamento.py` (apuração pura), `src/dados/acompanhamento.py` (leitura dos leads no Newcore) e `src/dados/registro/acompanhamento.py` (carga aprovada + gravação do apurado). Mede o que a carga de sexta produziu: leads por imóvel em posição paga e a lista de leads sem tratamento (Spec §4). **Registrado aqui apesar de acompanhamento não ser "regra de decisão"** porque a D-018 abaixo muda a definição que determina QUEM é nomeado na aba de cobrança — sem registro, dois relatórios de segunda seriam incomparáveis, que é exatamente o dano que a convenção deste arquivo existe para evitar.

### Changed

- **A janela da rodada de segunda passa a ser DERIVADA de `aprovada_em` da carga medida** (Spec §1), não mais escolhida por quem invoca, e **janela truncada é declarada como limitação na planilha** (§7.2). Registrado apesar de não ser "regra de decisão", pelo mesmo motivo da entrada de D-018 abaixo: muda o PERÍODO que o relatório mede. Os números já registrados aqui (lista de leads sem tratamento **197 → 162**) saíram de uma janela `28→31/08` escolhida a mão; sob a regra nova a janela sai de quando a carga foi aprovada e **pode vir truncada** — a primeira execução real mediu **0 de 3 dias**, e agora diz isso na planilha. Comparações entre segundas só valem entre rodadas sob a mesma derivação de janela.
- **D-018 — "atendimento registrado" passa a incluir o HISTÓRICO, não só o estado atual.** Decisão do dono (2026-09-01) após medição: `FT_Leads.AttendedAt` é campo de estado e é APAGADO quando o lead sai do atendimento (nulo em 100% dos leads `Removido`). A regra ingênua contava como abandono **46% de quem foi atendido** e superestimava a lista em **1,87×** em 90 dias (+21,6% na janela real). A regra adotada é `AttendedAt IS NOT NULL OR EXISTS(facstatushistory WHERE Fac_Id=? AND StatusAfter=12)` — o "critério mais conservador" que a própria §4.2 diz perseguir. **Efeito na entrega: a lista de leads sem tratamento cai de 197 para 162 na janela 28→31/08.** Comparações entre semanas só valem entre rodadas sob a mesma definição.
- **D-019 — "gestor de distrito" (Spec §4.2) é o EMBAIXADOR** (`FT_Leads.embaixador`, 23 pessoas), não o `AmbassadorManager` (2 pessoas para 1.616 distritos). Muda quem é nomeado na coluna de responsável distrital.
- **D-020 — as duas colunas da §4.3 sem fonte ficam VAZIAS e acumulam.** `adsrealtyextra_historic` está morta desde 27/06/2023; "semanas consecutivas em destaque" e "leads acumulados na janela atual" não têm origem no Newcore. Ficam `None` (ausência declarada, nunca zero) e se preencherão pelo Registro próprio — **cujo acúmulo ainda não tem produtor**, corte declarado em `src/dados/registro/acompanhamento.py`.

- **F3 (desempenho de portal) passa a ENTRAR no ranking a partir da raspagem — Coletor Externo fiado no grafo (G4).** A D-017 definiu o F3 como "desempenho de portal, reforço, zerado sem raspagem"; até aqui ele era literalmente `0.0` para todos (a piloto não raspa). Agora `decidir(..., desempenho_por_imovel=...)` recebe o sinal por imóvel, normalizado min-max entre a população como os demais fatores (`src/piloto/decisao.py`), e o nó `no_coletor_externo` (`src/grafo/fluxo.py`) o alimenta lendo a saída do raspador (`src/dados/coletor_externo.py`). O **default não muda** (sem raspagem injetada, F3 = 0, rodada DEGRADADA nesse fator). O que muda: com raspagem fresca e amarrada, F3 entra e a rodada pode ser COMPLETA. **Provisórios, NÃO adotados** (permanecem nulos em `src/config`): a composição do sinal F3 a partir de nota/visualizações/cliques (forma nº 2, em aberto — default run-local = visualizações), o limiar mínimo de amarração (nº 7) e a idade máxima da coleta (nº 5). A "reserva" da coleta velha (Spec §7.3) ainda não é reusada — coleta fora da janela degrada. Registrado em `docs/decisoes.md` (nota G4).
- **Crivo de auditoria da rodada — camada 1 determinística com VETO (D-017) — `src/dominio/auditoria.py` (novo).** Fecha o desenho D-017: um mecanismo que fiscaliza a seleção antes da entrega. `auditar(alocacao, itens)` é cálculo puro (stdlib, invariantes 4/5: mesmo resultado ⇒ mesmo veredito, sem modelo) e NÃO recomputa a decisão — recebe o resultado pronto e o confere de forma independente (pega o bug que a alocação não veria). Verifica: cotas 475/6.495 não excedidas (invariante 6), piso R$700k em todo super destaque (D-002), super destaque nunca vindo de relaxamento (invariante 7), nenhum imóvel em dois níveis, toda posição alocada com justificativa, todo super destaque ELEGÍVEL e todo destaque elegível-ou-recuperado (ressalva do revisor-de-regra), e o **corte do ranking honrado** (nenhum elegível excluído com nota acima do corte do seu nível — a forma verificável da "dominância", que a D-017 marcou como elaboração ajustável). Se qualquer verificação falha, a rodada NÃO fica pronta (veto), com a lista de violações (código estável + detalhe). **Camada 2 (parecer por modelo) NÃO é implementada aqui**: só o contrato é declarado (`AgregadosParaParecer` — só agregados, invariante 3/D-006, nenhuma identidade de pessoa), como pendência da fatia do Redator-com-modelo (provedor ainda não escolhido, Redator hoje template). **Emenda 2026-09-01 à D-017** (`docs/decisoes.md`): o dono ratificou a camada 2 DENTRO do Redator, sem ampliar o "três agentes usam modelo" — confirmação do default da D-017, não decisão nova (por isso emenda, não D-018). Wire da camada 1 no runner run-local (não commitado) para o dono ver o veredito. 17 testes (cenário pronto, cada violação isolada — incl. super/destaque inelegível, cotas super e destaque excedidas, recuperado não baixa o corte, empate no corte não viola, agregação de vetos, ambos vazios, determinismo, contrato da camada 2 sem identidade).

- **Redesenho dos fatores do ranking (regra de decisão) — D-017.** Sobrescreve os PESOS e o conjunto de fatores da Spec §6.3, **preservando os objetivos por nível** (valor esperado no super destaque, probabilidade de lead no destaque). Os quatro fatores passam a ser: **F1 semelhança com perfil PONDERADA POR DIMENSÃO** (peso decrescente preço > localização > metragem > dormitórios > vagas — corrige a saturação da primeira rodada real, em que 443/475 super destaques eram puxados pelo mesmo perfil de dormitórios); **F2 leads como sinal POSITIVO** (`norm(Leads180D)` — lead deixa de ser só penalidade §6.4 e vira sinal primário, realinhando a mecânica ao objetivo já declarado na §6.3); **F3 desempenho de portal como REFORÇO aditivo** (zerado sem raspagem, order-preserving, mantendo D-016); **F4 produtividade do gestor por INTENSIDADE CONTÍNUA** (substitui o binário da D-016, que era redundante com a regra de elegibilidade "gestor produtivo" e não discriminava). A análise primária é o **banco** (vendidos + mais leads), disponível sem raspagem; o portal reforça. **Os valores dos pesos do novo esquema e os pesos por dimensão do F1 passam a ser parâmetro NULO** (eram definidos na §6.3): decisão do dono ainda não tomada — entram como PROVISÓRIOS run-local injetados, nunca hardcode em `src/dominio`, nunca em `src/config` (invariante 5 exige pesos injetados). A **regra de elegibilidade "gestor produtivo"** e o **distrito ≥ 2** (Spec §6.1 + D-015) **não mudam**. Introduz o **crivo de auditoria híbrido**: camada 1 determinística com veto (`src/dominio/auditoria.py`, no caminho da decisão, cálculo puro — invariantes 4/5) + camada 2 consultiva por modelo, só agregados, fora do caminho da decisão (invariantes 3/4). Divergência declarada com a hierarquia: a **Spec §6.3 precisa ser atualizada**; até lá a D-017 prevalece sobre o trecho divergente. Ponto de calibração aberto (não alterado aqui): a penalidade "sem lead 180d" fica quase redundante com F2=0. Ressalvas dos portões incorporadas à D-017: os pesos agora-nulos e os pesos por dimensão do F1 passam a ser **parâmetros pendentes** — a tabela do CLAUDE.md e a lista da D-004 serão atualizadas na fatia 2 (evita divergência silenciosa com o "onze parâmetros" canônico); a **camada 2 consultiva é realizada dentro do Redator** (D-006, agregados), não um quarto agente com modelo, salvo decisão do dono ampliando o "três agentes" antes da fatia 3; F2/F4 herdam a normalização min-max PROVISÓRIA sobre os elegíveis (D-016); a "dominância" da camada 1 é elaboração de desenho, ajustável no code review da fatia 3. Esta fatia registra só a DECISÃO (docs); o domínio, o crivo e a fiação do piloto vêm nas fatias seguintes.

- **Domínio do ranking redesenhado (D-017, fatia 2) — `src/dominio/perfil.py` + `src/dominio/ranking.py`.** `perfil.py` ganha `PRIORIDADE_DIMENSOES` (a ordem adotada preço > localização > metragem > dormitórios > vagas — regra de decisão do dono, distinta da ordem canônica do enum que só serve à saída determinística) e `pesos_por_prioridade(decaimento)` — função PURA (invariantes 4/5) que gera o peso DECRESCENTE por dimensão a partir dessa ordem, com a MAGNITUDE do decaimento injetada (parâmetro nulo nº 13), nunca fixada; `decaimento=1.0` não de-satura, `<1.0` acentua a dominância de preço/localização. `ranking.py` ganha o **4º fator, `leads` (F2)**, em `FatoresNormalizados` (default 0.0 = dormente) e `PesosNivel` (`leads_positivo`), com `nota_bruta` incluindo o termo `leads_positivo × leads`; a soma dos pesos segue obrigatória em 100, agora sobre os quatro fatores. **Backward-compatible de propósito**: os pesos entram DORMENTES (leads_positivo=0) nas constantes-ponte `PESOS_SUPER_DESTAQUE`/`PESOS_DESTAQUE` — reetiquetadas de "definidas §6.3" para PROVISÓRIAS run-local (D-017: pesos agora nulos), preservando a costura já mergeada verde até a fatia da costura injetar o esquema vivo. A fiação viva de leads (`norm(Leads180D)`), a produtividade contínua (F4) e a aplicação do peso por dimensão (combinação de perfis de 2 dimensões) são da fatia da costura — que também deve **remover o consumo direto das constantes-ponte `PESOS_*` em `src/piloto/decisao.py` e injetar os pesos provisórios run-local**, fechando o mandato de injeção da D-017 (invariante 5: pesos injetados, não constantes escondidas). Até lá a dívida fica rastreada (não é divergência silenciosa: `nota_bruta`/`nota_final` já são injeção-capazes, só o chamador ainda importa as constantes). **Reconciliação da lista de parâmetros (débito da D-017 quitado aqui)**: a tabela do CLAUDE.md e a D-004 passam de ONZE para TREZE — nº 12 (pesos dos quatro fatores por nível) e nº 13 (decaimento por dimensão do F1), antes definidos na §6.3 e agora nulos; doze pendentes. 17 testes novos/ajustados (leads na soma ponderada, leads dormente não altera a nota, finitude de leads, prioridade adotada, decaimento decrescente e sua validação de faixa).

- **Costura do ranking redesenhado ligada (D-017) — `src/piloto/semelhanca.py` + `src/piloto/decisao.py` + `src/entrega/planilha_piloto.py`.** Liga o esquema novo na piloto (`decaimento`/pesos ainda PROVISÓRIOS run-local, nunca adotados): **(1) F1 ponderado por dimensão** — `_contribuicoes` passa a escalar a contribuição de cada perfil por `_peso_do_perfil` = MÁXIMO dos pesos das suas dimensões (leitura estrutural declarada; alternativas produto/média registradas), com o `decaimento` novo em `ParametrosSemelhanca` (nº 13). É a **de-saturação**: o perfil amplo de "dormitórios" (baixa prioridade) deixa de dominar (teste crava preço vencendo vagas quando `decaimento<1`). **(2) F2 leads vivo** — `_fatores` monta o fator positivo `leads = min-max(Leads180D)` **sobre os elegíveis** (D-016), com `Leads180D` já coletado (via `ImovelPenalizavel`, `_leads_do` falha alto se faltar); a planilha ganha a coluna `leads` na composição da nota. **(3) Injeção dos pesos — FECHA o mandato da D-017/invariante 5**: `ParametrosDecisao` passa a carregar `pesos_super`/`pesos_destaque` (`PesosNivel`) injetados; `decisao.py` **não importa mais** `PESOS_SUPER_DESTAQUE`/`PESOS_DESTAQUE` de `src/dominio` — a dívida rastreada na fatia 2 está quitada. As constantes-ponte foram **removidas de `src/dominio/ranking.py`** e relocadas para as fixtures de `tests/test_ranking.py` (ressalva do revisor-de-regra), honrando a letra da D-017 ("nunca hardcode em `src/dominio`"): o módulo `ranking.py` agora só define a ESTRUTURA (PesosNivel + soma ponderada), os VALORES chegam por injeção. **F4 produtividade segue BINÁRIO, intocado** — a versão contínua (D-017) depende de campo novo do coletor (`productivityrating`) e fica para fatia própria, declarado nas degradações da rodada. A aba de limitações passa a emitir os **valores** de `decaimento` e dos pesos por nível como linhas PROVISÓRIO (ressalva do revisor-de-regra: os números que produziram a lista visíveis para auditabilidade), ao lado de `desconto_fragil` e das intensidades; a forma min-max (nº 2) e as intensidades (nº 3) seguem rotuladas; nada adotado. +8 testes (de-saturação preço×vagas, perfil 2-dim vale pela dimensão mais importante, faixa do decaimento, F2 leads positivo discriminando a nota, coluna `leads` na planilha).

- **F4 produtividade do gestor: binário → intensidade CONTÍNUA (D-017) — `src/dados/coletor_interno.py` + `src/dominio/elegibilidade.py` + `src/piloto/decisao.py`.** Acende a parte que a costura deixou dormente: o fator de ranking F4 deixa de ser o binário morto (que era redundante com a REGRA de elegibilidade "gestor produtivo" e não discriminava ninguém) e passa a uma intensidade contínua normalizada. `ImovelCandidato` ganha o campo ADITIVO `produtividade_gestor_30d` (o binário `gestor_captou_ou_vendeu_30d` permanece, intocado, servindo só à elegibilidade §6.1); `_fatores` normaliza esse campo (min-max sobre a população, D-016) em vez do binário. **Limitação de dados declarada (medida, mapa-de-dados):** a base NÃO expõe contagem de captações nem de vendas em 30 dias — a única métrica genuinamente de 30d é `Captations_per_week_last_30d` (taxa semanal, 0–15, 99,5% preenchida) e `Sells` é de 365 dias, não 30d. Então o sinal contínuo = `COALESCE(Captations_per_week_last_30d,0) + COALESCE(LastSell≥30d,0)`: captação como taxa contínua + venda como flag binário de recência (base sem contagem de vendas em 30d). A limitação vai declarada nas degradações da rodada e no mapa de dados (armadilha nova: `productivityrating.Sells` é 365d). **Invariante 1**: leitura nova é só uma EXPRESSÃO sobre `productivityrating`, tabela JÁ no JOIN do coletor — nenhuma tabela nova, SELECT sob a guarda read-only. Sem novo D-0xx (D-017 já autoriza F4 contínuo). +1 teste (F4 contínuo discrimina a nota) + fixtures ajustadas.

### Added

- **Camada Python do Registro — persiste a rodada e as decisões (G2a)** (`src/dados/registro/conexao.py` + `escrita.py` + `leitura.py` + migração `002_leads_no_registro.sql`). A **PRIMEIRA escrita real do sistema** (invariante 2): a rodada de decisão inteira — a rodada + as 6.970 posições (notas dos QUATRO fatores D-017, penalidades, perfil que puxou, regra relaxada) + o relatório de relaxamento + os parâmetros — persiste numa **transação única** (falha parcial não deixa rodada meio-gravada) no PostgreSQL PRÓPRIO, esquema `registro`. Grava EXCLUSIVAMENTE nesse banco: `src/dados/registro` não importa `newcore.py` (invariante 1) e `src/dominio` não importa o Registro (invariante 4 — persistir é I/O fora do domínio, *depois* do caminho determinístico). **Migração 002**: o DDL 001 é anterior à D-017 e só tinha três notas de fator (perfil/desempenho/gestor); adiciona `nota_leads` a `decisao_imovel` (idempotente, `ADD COLUMN IF NOT EXISTS`) — senão a persistência perderia o F2. Conexão por `POSTGRES_URL` com fail-fast, como o `newcore.py` (um Postgres, uma URL, dois esquemas — Registro + checkpointer do grafo). As **cotas 475/6.495 (invariante 6) e o "super nunca relaxa" (invariante 7) são reforçados pelos CHECKs do próprio DDL** — segunda linha de defesa além do domínio, com teste cravando a rejeição de cota pelo banco. Idempotência: cada chamada = uma rodada (`rodada.id` IDENTITY, sem chave natural); deduplicar re-execução da mesma rodada lógica fica para o chamador/G2b, declarado, não inventado. **Migração 003**: o déficit de destaque ("posições ainda vazias", Spec §2.1) — que a `escrita` gravava sempre 0, perdendo o `ResultadoRelaxamento.deficit_restante` — passa a persistir em `rodada.posicoes_vazias_destaque` (o total é grandeza da rodada, não por regra cedida); ressalva do revisor-de-regra, corrigida, não declarada como perda. Leitura mínima (`ler_rodada`, `contagem_por_nivel`, `marcar_aprovada` com checagem de `rowcount`) sustenta a rodada de segunda (D-001: o Registro é a fonte da verdade). Guard `autocommit=False` na escrita (atomicidade não quebra em silêncio). Adiciona `psycopg[binary]` às dependências (`uv.lock` atualizado). O **nó de persistência no grafo** fica para fatia própria (G2a-wire); a integração foi provada por runner run-local (475/6.495 gravados e relidos). **Cortes de escopo DECLARADOS (docstring de `escrita.py`, não silenciosos)**: `perfil_id`/`perfil_da_rodada` não persistidos (só a evidência do perfil, sem o identificador — fatia com o Analista de produção); `tentativas_por_etapa` vazio (depende do retry do Orquestrador, nº 4 nulo); sem controle de versão de schema (as `NNN_*.sql` aplicadas à mão — pendência para a G2b). 7 testes de integração (round-trip com déficit, os quatro fatores, aprovação tácita, CHECK de cota, relaxamento persistido — regra cedida + linha de relaxamento + déficit residual, CHECK super-nunca-relaxa, round-trip de parâmetros) — I/O, rodam contra o Postgres local e **pulam no CI** sem `POSTGRES_URL`, como o padrão dos testes de I/O do projeto.

- **Esqueleto do grafo LangGraph — marco F, G1** (`src/grafo/estado.py` + `src/grafo/fluxo.py`). A rodada de decisão de sexta como grafo: Coletor Interno → (Analista de Perfil ∥ Coletor Externo) → Decisor → **crivo (gate de "pronto")** → Redator → finalizar (Spec §5, ordem da sexta). Cada nó embrulha um módulo já testado e escreve seu produto + seu "pronto" no estado; **nenhuma regra de decisão vive no grafo** — `langgraph` é importado SÓ em `src/grafo/`, o domínio segue stdlib puro (invariante 4, o auditor verifica). Estados da rodada (Spec §7.2) derivados honestamente: **abortada** se a coleta interna vem vazia (sem estoque, sem decisão), **degradada** se alguma fonte falta, **completa** só com tudo pronto. Na G1 o **Coletor Externo é stub** (sem raspagem) → a rodada nunca marca completa, é **degradada** com a ausência declarada; o Analista roda por contagem (determinístico) e o Redator não gera resumo por modelo. O **crivo camada 1** entra como gate de pronto da decisão: se veta (seleção viola cota/piso/relaxamento), a rodada **ABORTA e NÃO entrega** — um `_rota_pos_crivo` desvia do Redator ("etapa sem pronto não entrega para a seguinte"). NOTA de desenho levada ao dono: a Spec §7.2 só prevê ABORTADA para coleta interna vazia; a G1 reusa ABORTADA pela consequência (sem entrega), com o motivo distinguindo veto-de-integridade de estoque-vazio — a §7.2 pode ganhar um estado próprio numa revisão. As **fontes de dado são injetadas** (`Fontes`), então o grafo roda em teste sem MySQL; contra o banco real produz 475/6.495 nas cotas com crivo PRONTA e estado DEGRADADA. Adiciona `langgraph` às dependências (`uv.lock` atualizado). **G1 compila SEM checkpointer** (o estado carrega objetos de domínio não-serializáveis; o checkpointer Postgres + serialização + a interrupção de aprovação são a G2/G3, e o retry do Orquestrador — parâmetro nº 4, nulo — é fatia própria). Nenhuma regra/parâmetro alterado; nenhum valor nulo inventado. 11 testes (fluxo degradado ponta a ponta, cotas, aborto por estoque vazio, perfil sem robustez degrada sem abortar, crivo válido pronto, crivo veto → aborta sem entregar, roteamento pós-crivo, determinismo de super E destaque, `estado_final`).

- **Planilha-piloto em CSV** (`src/entrega/planilha_piloto.py`): serializa o `ResultadoDecisao` em quatro CSVs (super_destaque, destaque, excluidos_por_regra, parametros_e_limitacoes) que mapeiam 1:1 nas abas do Google Sheets (B4). **Serializa, não recomputa**: nota, fatores, cada penalidade, perfil que puxou e degrau de relaxamento são lidos do resultado da costura — teste crava que as linhas batem campo a campo com o `ResultadoDecisao`. Traz as colunas de composição/justificativa da decisão (Spec §2.1/§3.2: notas dos três fatores, valor de cada penalidade, nota final, perfil que casou, contexto de relaxamento) — as colunas descritivas e de portal (título, link, endereço, métricas do portal) vêm dos coletores/Redator no entregável final, não do Decisor; a aba de parâmetros registra essa nota. Aba de parâmetros rotula os provisórios como PROVISÓRIO e lista as quatro limitações da rodada. **Invariante 3**: só imóvel/posição/notas/perfil, nenhuma identidade. Saída em `saida/piloto/` (ignorada pelo .gitignore — dado de rodada, nunca commitado). `ResultadoDecisao` ganha `reprovados_regras` (por imovel_id), carregado do split de elegibilidade, para a aba de excluídos não recomputar elegibilidade. 7 testes (puros, sem arquivo, + 1 com tmp_path).

- **"Perfil que puxou" na justificativa** (`src/piloto/semelhanca.py` + `decisao.py`): `DetalheImovel` passa a carregar o perfil de conversão de maior contribuição que o imóvel casou (Spec §2.1: identificador e evidência do perfil casado), fechando a dívida rastreada da planilha justificada. `semelhanca.py` extrai `_contribuicoes` como FONTE ÚNICA — `sinal_bruto` (=max) e o novo `perfil_que_puxou` (=argmax) derivam dela, então o número e o perfil exibido nunca divergem (teste crava a concordância). Empate de contribuição: ganha o mais específico (mais dimensões), depois a ordem canônica — rótulo explicativo, não decisão distributiva (não muda o número, o ranking, nem quem é alocado). None se o imóvel não casa nenhum perfil. 8 testes novos.

- `src/piloto/decisao.py`: a COSTURA da decisão da piloto — encadeia como cálculo puro (invariantes 4/5) as seis etapas do domínio (elegibilidade → fatores → penalidades → ranking → alocação → relaxamento). **D-016** registra as três leituras estruturais: (1) `desempenho_proprio`=0 para todos — a piloto não raspa o portal, rodada DEGRADADA nesse fator (order-preserving); (2) `produtividade_gestor` = sinal binário `gestor_captou_ou_vendeu_30d`, NÃO zera; (3) **normalização (forma do parâmetro nº 2, provisória) SOBRE OS ELEGÍVEIS** no ranking primário — normalizar sobre todos faria o ranking das 475/6.495 posições depender de imóveis reprovados que nunca serão colocados; reprovados são normalizados entre si no relaxamento (invariante à escala, saída idêntica). Cotas (invariante 6) e relaxamento só-destaque (invariante 7) vêm dos módulos do domínio, não reimplementados. Provisórios nº 2/nº 3 injetados run-local (`ParametrosDecisao`), nunca em `src/config` (D-014). Saída (`ResultadoDecisao`) carrega alocação, relaxamento, o **detalhamento por imóvel** (`DetalheImovel`: os três fatores, o desconto de CADA penalidade e as notas — carregados da costura, não recomputados no B3c, para a planilha justificada da Spec §2.1/§3.2/§6.4) e as degradações declaradas para a aba de limitações. Para expor o valor por penalidade, **`src/dominio/penalidades.py` ganha `descontos_por_penalidade`** (dict por penalidade, autoritativo; `desconto_total` passa a somá-lo — comportamento preservado, breakdown e total nunca divergem). O "perfil que puxou" (Spec §2.1) fica como incremento rastreado (exige `semelhanca` expor o match). 13 testes da fiação + do breakdown.

- `src/dados/bucketizacao.py` (FONTE ÚNICA) + `src/dados/candidatos_perfil.py`: a leitura das cinco dimensões de perfil do imóvel CANDIDATO, para casá-lo com os perfis de conversão. **Refactor:** a bucketização (faixas de preço, colapso de dormitórios/vagas, texto) sai de `vendas.py` para `bucketizacao.py` — um único lugar que venda e candidato importam, para que o mesmo valor caia sempre no mesmo bucket (senão o match zeraria a semelhança para todos, sem erro); comportamento preservado, asserções migradas para `test_bucketizacao.py` com valores idênticos. **Leitura do candidato** (verificada no banco, 31/08): região=`FT_RealtyRelation.District` (66/66 distritos das vendas), metragem=`PrivateArea_Range` nativo (mesmos 8 rótulos, sem bucketizar), dormitórios=`QtyBedrooms`, preço/vagas de `realties` por JOIN — casando string-a-string com o lado venda. **Armadilha registrada:** `FT_RealtyRelation.Price_Range` é IGNORADO (vocabulário incompatível; casaria zero) — a faixa de preço vem sempre de `realties.Price`+`faixa_de_preco()` nos dois lados. Teste cruzado prova a fonte única. **Invariantes 1/3**: SELECT só leitura via guarda, só características de imóvel. **Regra de config:** as faixas de preço não são um dos onze parâmetros da D-004, mas AFETAM a decisão (alimentam `semelhanca_perfil`, peso 60/80) — mudança nas faixas exige entrada neste CHANGELOG (docstring de `bucketizacao.py` corrigido nesse sentido). 12 testes (I/O não roda no CI).

- `src/dominio/perfil.py` + `src/dados/vendas.py`: o Analista de Perfil v0 (determinístico) descobre os perfis de conversão da Spec §6.2. **`perfil.py` é função PURA** (invariantes 4/5): recebe os atributos dos imóveis vendidos (identidades já removidas), conta combinações de **uma e duas** dimensões (região, faixa de preço, faixa de metragem, dormitórios, vagas — nunca três ou mais) e devolve cada perfil com o nº de vendas que o sustenta, em ordem canônica. Aplica a **evidência mínima N ≥ 3 (D-014)** como rótulo de **fragilidade, não de exclusão** (Spec §6.2: frágil "não recebe peso pleno"): perfil frágil continua no resultado, marcado. NÃO produz `semelhanca_perfil` normalizada — isso é a costura (B3), a montante do ranking (parâmetro pendente nº 2, nulo). `vendas.py` é a I/O que monta `ImovelVendido`: lê as vendas assinadas em 180d de `FT_LeadsOffers` (D-013; colunas de perfil da própria oferta — cobertura melhor que `FT_RealtyRelation`, ver mapa), preço/vagas via JOIN em `realties`. **Invariante 3**: só características de imóvel, nenhuma identidade. Bucketização (faixa de preço ancorada nos pisos da Spec §6.1; colapso de dormitórios/vagas) mora na coleta, não no domínio. 20 testes (13 da descoberta pura + 7 das conversões; a I/O não roda no CI). Mapa de dados registra a fonte e a cobertura por dimensão.

- `docs/decisoes.md` D-013, D-014, D-015 (decisões do dono, 31/08/2026), ancoradas na distribuição de vendas por perfil medida em leitura pura do Newcore: **D-013** — "venda assinada em 180 dias" = 177 ofertas (`FT_LeadsOffers.SignedAt` não nulo nos últimos 180d), **inclui** as 6+1 posteriormente canceladas; a leitura "líquida" (171) fica como alternativa não escolhida. **D-014** — parâmetro pendente nº 1 (evidência mínima por perfil) resolvido em **N ≥ 3** (piso único): a localização é o gargalo do sinal — cobertura das vendas cai de 69% em N≥3 para 32% em N≥5, e localização×dormitórios zera em N≥8; abaixo de 3 um bucket sobre 177 vendas é coincidência. **D-015** — `DefinicaoAtivoDistrito` = `BrokersProductivity` ("corretor ativo" = captou ou vendeu em 30 dias): **fiel ao texto literal da Spec §6.1** (que já define a regra por extenso — não era lacuna, era mapeamento de coluna); cobertura de distritos com ≥2 = 45,9%, portanto **muda quem é elegível**. A alternativa "logou em 30d" (76,8%) foi descartada por divergir da Spec. Efeito colateral na D-004: o nº 1 sai da lista de pendentes (dez seguem nulos); a **tabela de parâmetros do `CLAUDE.md`** marca nº 1 resolvido e reafirma que os provisórios nº 2/nº 3 da planilha-piloto são run-local, PROVISÓRIOS e **não adotados** — seguem nulos, nunca em `src/config`.

- `src/dados/newcore.py` + `src/dados/coletor_interno.py`: o Coletor Interno lê o MySQL do Newcore e monta os contratos do domínio (`ImovelCandidato`, `ImovelPenalizavel`) — camada de I/O que alimenta `src/dominio`, sem tocá-lo (os dataclasses frozen são a fonte da verdade; zero drift). **Invariante 1** (Newcore só leitura) em profundidade: credencial de usuário somente-leitura (a defesa dura, `NEWCORE_MYSQL_USER`) + guarda de código que só aceita SELECT/SHOW. **Conexão**: pymysql com a senha em `bytes` UTF-8 — resolve o `1045 "só mysql2 autentica"` aberto desde a fundação (a senha tem U+00A8; pymysql força latin-1 em senha `str`, mandando o byte errado; ver mapa de dados). Leitura mínima (invariante 3): só os campos que os dataclasses exigem, nenhum nome/contato pessoal. Fontes de cada campo confirmadas contra a base em 31/08. Decisão pendente do dono declarada em `DefinicaoAtivoDistrito`: qual coluna de `FT_Districts` conta como "corretor ativo no distrito" (4 opções, funil de 94,8% a 45,9%). `janelas_anteriores` vêm do Registro (Postgres), não do Newcore — vazias na primeira rodada. FT_RealtyRelation é 1:1 por Realty_Id (48.985 linhas = 48.985 imóveis, verificado 31/08) e a query não infla; dedup por imovel_id como rede contra deriva futura; ORDER BY Realty_Id para entrada canônica; a guarda de leitura bloqueia WITH (CTE de escrita no MySQL 8). Runtime ganha pymysql+cryptography (src/dominio segue stdlib puro). 11 testes das conversões puras (a I/O não roda no CI).

### Fixed

- **`vendas.py` quebrava com `Realty_Id` nulo** (bug pego pela PRIMEIRA rodada real da piloto, não pelo CI — as fixtures não tinham o nulo que o dado real tem): algumas ofertas assinadas em 180d têm `Realty_Id` nulo, e `linha_para_vendido` fazia `int(None)`. Uma venda sem imóvel é estruturalmente inutilizável para o perfil (sem preço/vagas do JOIN, sem dimensões para casar, sem `imovel_id` para ancorar). Correção: `_vendas_ancoraveis` separa e **CONTA** as descartadas (nunca em silêncio); `coletar_vendas` devolve `(vendas, nº descartado)`; `linha_para_vendido` falha alto (fail-loud) se um nulo chegar (tripwire de regressão). A contagem é devolvida por `coletar_vendas` e o mecanismo para declará-la na aba de limitações existe e é testado nas duas pontas (`notas_coleta` em `escrever_planilha`/`linhas_parametros_e_limitacoes`); o runner da rodada faz a ligação produtor→consumidor. **D-013 não muda**: venda=177 continua a métrica; o perfil é descoberto sobre as vendas ANCORÁVEIS (177 menos os nulos) — robustez de leitura, não regra. Fixture com `imovel_id` nulo fecha o buraco do CI. 4 testes novos.

- **Transporte do coletor Canal Pro** (validado no canário de 31/08/2026): a API `gandalf-api` autentica por header (Bearer), não por cookie — uma requisição POST cross-origin **com** credenciais é rejeitada no preflight CORS (`credentials: 'include'` → status 0), **sem** credenciais retorna 200. `fetchInPage` ganha `credentials` configurável (default `'include'` mantém compat) e o adapter Canal Pro passa `'omit'` explícito (teste trava a regressão). `fetch` sem credenciais basta — não foi preciso trocar por XHR. `pageSize` 30 → 100 (o máximo que o canário validou): ~551 requisições em vez de ~1.836 contra o portal, menos superfície anti-bot. Recipe atualizado de "risco em aberto" para "transporte validado", com os números agregados do canário (sem header/token/dado real).

### Added

- `coletor-externo/src/portals/canalpro.ts`: adapter real do Canal Pro, substituindo o stub. API GraphQL (`POST gandalf-api.grupozap.com`, operationName `listings`), paginação linear por página. **Query MÍNIMA (invariante 3)**: só performance de portal e amarração — nota (`score`/`scoreName` cru, sem reescala), visualizações e cliques por tipo (5 colunas separadas, não somadas, contagens agregadas), preço, nível, `createdAt`, portais, e `externalId` (amarração com o imóvel interno); **nunca** endereço, geo, imagens ou contato de lead (o CSV alimenta o Analista de Perfil, que usa modelo) — há teste que quebra se a query passar a pedir esses campos. Sessão do portal = conjunto de headers de auth, tratada como tipo OPACO (`Sessao`) pelo núcleo e nunca logada/persistida (segredo). `run.ts` ganha caminho de paginação linear com **checkpoint por página** (`Checkpoint.lastPage`): a retomada continua de `lastPage+1` em vez de re-bater o portal do início (condição anti-bot). `fetchInPage` estendido para POST (GraphQL). `CANALPRO_SCRAPE_RECIPE.md` documenta a API (nomes de header, nunca valores — D-012). Contrato de saída `Anuncio` expandido com os campos que o reconhecimento achou. 12 testes novos (parsing, ausências→null, guardião do invariante 3, csvColumns, classificação de bloqueio→BlockedError/AuthExpiredError, chave de ordenação imutável). Tratamento de falha: `postListings` classifica a resposta pelos detectores de bloqueio ANTES de ler dados — bloqueio duro vira `BlockedError` (dispara NEEDS_WARM/exit 2), 401 vira `AuthExpiredError`, anomalia vira erro explícito; nunca lista vazia silenciosa marcada 'ok'. Ordenação por `CREATED_AT` (imutável) para retomada estável. Risco declarado no recipe: o transporte POST cross-origin não foi validado por injeção (CORS + Datadog RUM); o canário do coletor via CDP o valida. URL do anúncio não vem na listagem (lacuna registrada).

### Added

- `docs/decisoes.md` D-010, D-011, D-012 (decisões do dono, 31/08/2026): **D-010** — o Coletor Externo adota transporte CDP em Chrome real (anexa a um Chrome autenticado pelo operador, chamadas à API do painel de dentro da página; login humano único), modelado no raspador `imovelweb-ativos` validado pelo dono; registrado no nível arquitetural (a receita executável vive no código, D-012), substitui Selenium por propriedade da técnica e NÃO re-atribui o incidente do task-titan / raspagem do Grupo Zap (que segue como infraestrutura no mapa de dados, por decisão do dono). **D-011** — o produto ganha um Console do Operador (front-end de operação/observabilidade) que lê o Registro/checkpointer e escreve só no Postgres próprio (invariantes 2 e 4 preservados); divergência com a hierarquia PRD>Spec declarada e não resolvida aqui (PRD e Spec precisam incorporar o console); entidade nova `custo_execucao` (nona, onde a Spec §2.1 define oito) com DDL em PR próprio; prompts dos agentes com modelo versionados por rodada. **D-012** — publicação aberta do coletor no repositório público: risco assumido pelo dono de forma informada (as três consequências irreversíveis apresentadas antes da escolha), análogo à D-001. Nenhum parâmetro pendente da D-004 recebeu valor.

- `src/dominio/relaxamento.py`: cedência progressiva e MÍNIMA da Spec §6.6 como cálculo puro — desce a ordem de cedência importada de `elegibilidade` (fonte única; um teste quebra se o módulo ganhar ordem própria), grau a grau, parando assim que o déficit zera; o degrau mínimo de um imóvel é o maior índice entre suas regras reprovadas, e quem reprova em regra não relaxável nunca entra. Relatório obrigatório como parte inseparável do resultado (Spec: sem ele a etapa não é "pronta"), listando apenas graus efetivamente cedidos — linha com zero para grau cedido sem recuperados, nenhuma linha para grau não alcançado. Déficit residual legítimo. Invariante 7 por construção: nenhum caminho de super destaque na assinatura, resultado sem nível (testado). Guarda de fronteira do invariante 6: déficit acima da cota de destaque é erro. Seleção dentro do grau por (-nota, -imovel_id) — não é leitura nova: sob cedência progressiva gulosa o pool de cada grau é exatamente quem depende dele, e reordenar o acumulado por nota daria o mesmo resultado (as duas leituras coincidem); o desempate é o da D-009. **Consequência distributiva declarada, decorrente da ordem da Spec**: nota alta que exige grau 3 perde para nota baixa que exige grau 1 — ponto de atenção ao dono. Nenhum teste usa os ganhos medidos (+133/+569/+1.680/+1.747/+5.686) como valor exato: D-005 — ordem de grandeza medida com mínimo de três corretores, parâmetro adotado é dois. 19 testes.

- `src/dominio/alocacao.py`: as duas fases da Spec §6.5 como cálculo puro — super destaque primeiro (piso de R$ 700.000 importado de `elegibilidade`, ordenação por nota do nível, cota 475), depois destaque entre todos os restantes (cota 6.495). Invariante 6 por construção (corte por fatia) e por amarração: teste que compara as cotas do módulo com o CHECK `posicao_dentro_da_cota` do DDL do Registro — os dois lados do contrato OLX quebram juntos ou não quebram. Déficit é resultado legítimo, sem preenchimento artificial (o relaxamento, módulo futuro, é quem o ataca). Leitura estrutural declarada: desempate por imovel_id crescente — total e determinístico, mas NÃO neutro: em todo empate favorece o cadastro mais antigo (ponto de calibração para o dono; **invertido pela D-009, ver Changed**). Precondição documentada: a entrada JÁ é o conjunto elegível; o módulo não reaplica as oito regras. 19 testes, incluindo determinismo sob permutação da entrada.

- `src/dominio/ranking.py`: nota final por nível (Spec §6.3) como cálculo puro — os dois conjuntos de pesos DEFINIDOS como constantes validadas (60/25/15 super destaque, 80/10/10 destaque; soma 100 obrigatória, inteiros não negativos — a fronteira vale para a futura rehidratação via alteracao_parametro) e a soma ponderada literal menos o desconto de penalidades. A forma de normalização dos fatores (parâmetro pendente nº 2 da D-004) fica fora do módulo: fatores chegam já normalizados e só a finitude é validada — faixa nasce com a normalização, quando o dono a definir. Capacidade de distrito não participa do ranking (PRD: o distrito já atua como regra eliminatória). Leitura estrutural declarada: soma ponderada sem divisão pelo total (constante absorvida pela calibração dos nº 2 e nº 3); contrato de desempate determinístico declarado para a alocação. 26 testes, incluindo integração com `dominio.penalidades`.
- `docs/decisoes.md` D-008 (regra de decisão): a nota ponderada de cada nível é a operacionalização do objetivo daquele nível e é a chave de ordenação da alocação — resolve a ambiguidade entre Spec §6.3 e §6.5. Ratificada pelo dono em 29/08/2026.

- `src/dominio/penalidades.py`: as três penalidades da Spec §6.4 como predicados puros — janela anterior sem resultado, sem avaliação por categoria (ausência TOTAL; a assimetria do parcial segue registrada na D-007), sem lead em 180 dias — e o desconto da nota com intensidades e decaimento **injetados como argumentos obrigatórios, sem default** (parâmetro pendente nº 3 da D-004 permanece nulo; `src/config/` segue sem código). Caso explícito da Spec coberto: imóvel sem histórico de destaque não é penalizado por ausência de histórico. Leituras estruturais declaradas: o julgamento "atingiu o resultado esperado para o nível" chega pré-calculado na entrada, porque nenhum documento o quantifica (pendência apontada ao dono da decisão); a penalidade por janela aplica-se uma vez, dirigida pela janela sem resultado mais recente; o fator de decaimento é contratualmente limitado a [0, 1] ("decai" nunca amplifica nem vira bônus) e as intensidades exigem valor finito e não negativo — intensidade negativa converteria penalidade em bônus e NaN quebraria a ordenação total do ranking. 30 testes.

- `docs/decisoes.md` D-007 (regra de decisão): na regra de cadastro completo, apenas zero explícito reprova; categoria ausente não é zero. Ratificada pelo dono com fundamento empírico — sob a leitura estrita, zero imóveis avaliados passariam e o funil medido do PRD seria impossível. Assimetria do parcial sem penalidade registrada como calibração futura (parâmetro nº 3).

- `src/dominio/elegibilidade.py`: as oito regras eliminatórias gerais como funções puras e determinísticas (D-002/D-003 — o piso de R$ 700.000 é função separada de candidatura ao super destaque, consumida na alocação; status impeditivo fica na rotação), a ordem de cedência do relaxamento (Spec §6.6) e `elegivel_com_relaxamento` que rejeita cedência de regra não relaxável. Limiares conforme parâmetros definidos: R$ 300.000, 10 fotos, 90 dias, 30 dias, 2 corretores. Leitura registrada para avaliação parcial por categoria: apenas zero explícito reprova cadastro completo; categoria ausente não é zero (imóvel sem avaliação alguma passa e recebe penalidade, Spec §6.1). 28 testes com valores-limite, contratos explícitos (caixa/acento de categoria, data futura documentada, mensagem de erro determinística) e verificação de determinismo. Contrato de `ImovelCandidato`: instâncias não são hasháveis (deduplicar por `imovel_id`) e o mapping de notas é copiado na construção — mutação externa não vaza.
- Scaffolding Python: `mise.toml` (Python 3.12), `pyproject.toml` (uv; pytest e ruff como dev; sem dependência de runtime — o domínio é stdlib puro por desenho) e `uv.lock`.

- `src/dados/registro/001_registro.sql`: modelo de dados do Registro — as 8 entidades da Spec §2.1 em esquema `registro` (rodada, parametros_da_rodada, perfil_da_rodada, decisao_imovel, relaxamento, janela_destaque, resultado_carga, alteracao_parametro). D-001 aplicada (aprovação tácita como carimbo de estado na rodada; nada modela leitura de volta da planilha). Parâmetros pendentes preservados nulos: sem TTL/expurgo (retenção, nº 9) e sem prazo em DEFAULT/CHECK (aprovação tácita, nº 10). Imóveis excluídos não são guardados, conforme decisão explícita da Spec. O esquema impõe os invariantes 6 e 7: CHECK de faixa por nível (1–475 super destaque, 1–6.495 destaque) que, com o UNIQUE (rodada, nível, posição), limita a contagem à cota contratada sem trigger — se o contrato OLX mudar, as cotas exigem ALTER TABLE; e CHECK que impede `regra_relaxada` em super destaque. Sem `modo_aprovacao`: nenhum documento define aprovação explícita (D-001, Ferramentas §4), restando `aprovada_em` só na rodada de decisão.

- `docs/decisoes.md`: registro das resoluções do dono da decisão (D-001 a D-006) para as contradições encontradas na fundação — fonte de leitura da segunda-feira é o Registro e não a planilha (o gestor não edita, só aplica); piso de R$ 700.000 como condição de nível; status impeditivo como regra de saída; lista consolidada de onze parâmetros pendentes; ganhos de relaxamento como ordem de grandeza; modelo do Redator restrito a agregados.

- Fundação do repositório: estrutura de pastas (`src/grafo`, `src/dominio`, `src/dados`, `src/entrega`, `src/config`, `tests`), sem código de produto.
- `CLAUDE.md` com projeto, cadência, stack, os sete invariantes, hierarquia dos documentos, glossário e os nove parâmetros pendentes (todos nulos).
- `docs/` com os três documentos-fonte (PRD 5.0, Spec 1.0, Ferramentas 1.0) movidos da raiz, e `docs/mapa-de-dados.md` derivado da base factual do PRD.
- `bug.md` com formato de registro de defeitos.
- Subagentes de desenvolvimento em `.claude/agents/`: `revisor-de-regra`, `investigador-de-dados`, `revisor-de-codigo`, `auditor-de-invariantes`, `conferente-de-numeros`.
- Skills de desenvolvimento em `.claude/skills/`: `consultar-newcore`, `verificar-contra-spec`, `registrar-bug`.
- `.gitignore` e `.env.tmpl` (segredos via 1Password, `op://Personal/orquestrador_portais/<VAR>`).

### Changed

- **D-009 verificada e confirmada na base (31/08/2026)**: `realties.Id` cresce com `CreatedAt` — zero inversões no estoque recente (41.969 pares), 0,092% na tabela inteira (jitter de segundos/horas e legado 2017, Ids ≤ 556, com a consequência declarada na decisão). Emenda datada em `docs/decisoes.md` preservando a ressalva histórica; docstring de `src/dominio/alocacao.py` atualizado (afirmava "ainda não verificado" — divergência código × documento corrigida, sem mudança de comportamento). `docs/mapa-de-dados.md` ganha as armadilhas de 31/08: `FirstActivationDate` não é proxy de ordem; `Id` ordena mas não conta (~36 mil deleções); JOIN de vendas perde 11,3% (impacto no parâmetro nº 1); colunas de views de portal quase mortas (impacto no fator de desempenho e no parâmetro nº 2); `adsrealtyscores` vazia; raspagem parada — diagnóstico de 31/08: incidente de infraestrutura (Chrome `session not created` desde 28/08 16:00 UTC nos três jobs do `webscrapper_cron` (11 falhas consecutivas, Ids 320–330, medição até 31/08 ~17:05 UTC); cadência histórica diária com fins de semana; gaps de até 6 dias sempre recuperados por backfill; em `webscraping_processing_grupo_zap` o `Id` não é monotônico com a data do relatório — datar por `ScrapedAt`; `webscrapper_cron` em UTC com falha crônica silenciosa em ~78% dos disparos); derivas datadas. Nenhum parâmetro pendente recebeu valor.
- **Regra de decisão (D-009, decisão do dono)**: o desempate da alocação foi INVERTIDO — em empate de nota, ganha o cadastro mais novo (instrução literal: "O desempate é preferencia por cadastros mais novos"). Implementação: proxy `imovel_id` decrescente nas duas fases de `src/dominio/alocacao.py`, sob pressuposto declarado e não verificado de que `imovel_id` cresce com a data de cadastro no Newcore — verificação pendente via `investigador-de-dados`; alternativa fiel (campo de data de cadastro) e atalho proibido (`atualizado_em`) registrados na D-009.
- `docs/mapa-de-dados.md` atualizado com as medições de 29/08/2026 que encerraram três das quatro investigações abertas: causa dos ~44% sem avaliação por categoria (pipeline de `realty_score_category_score` morto desde 16/10/2025 — a penalidade atingirá sistematicamente o estoque novo); tabela de relatórios da raspagem identificada (`webscraping_report_grupo_zap`, abandonada); **defeito 5 corrigido** — vagas existe em `realties.QtyVacancies` (96,1% com vaga >0, 0,4% nulo no recorte elegível). A quarta investigação (referência usada por quem aplica a carga) segue aberta como pergunta de processo. Skill `consultar-newcore` e agente `investigador-de-dados` alinhados.
- **Regra de decisão (interpretação)**: elegibilidade passa a ser lida como oito regras gerais + piso de nível do super destaque + gatilho de saída imediata (D-002/D-003). Nenhum limiar mudou de valor; mudou a estrutura de aplicação. Refletido em `CLAUDE.md` e no subagente `revisor-de-regra`.
- `CLAUDE.md`: lista de parâmetros pendentes consolidada de nove para onze (D-004); hierarquia de documentos passa a incluir `docs/decisoes.md`.
