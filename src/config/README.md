# src/config

Parâmetros de decisão. Toda alteração de parâmetro é registrada com data, autor e valor anterior (entidade `alteracao_parametro` do Registro) e no `CHANGELOG.md`.

Nove parâmetros estão pendentes e devem permanecer nulos até serem definidos — a lista está no `CLAUDE.md`. Nenhum pode ser preenchido com valor inventado.

Vazio por decisão: nenhum código de produto é escrito antes da fase de implementação.
