# Template do ambiente do console (gerado, não editado à mão):
#   op inject -i .env.tmpl -o .env
# O .env é ignorado pelo git; só este template é commitado. Convenção do repo
# (CLAUDE.md): segredos vêm do 1Password, cofre Personal.
POSTGRES_URL=op://Personal/orquestrador_portais/POSTGRES_URL
